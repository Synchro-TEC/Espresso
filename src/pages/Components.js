import React, { Component } from 'react';
import { SvRow, SvCol } from 'syntec-apollo-11';

class Components extends Component {
  render() {
    return (
      <SvRow>
        <SvCol>Components</SvCol>
      </SvRow>
    );
  }
}
export default Components;
